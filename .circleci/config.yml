version: 2.1

jobs:
  instance_creation:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout
      - run:
          command: |
            aws cloudformation deploy \
            --stack-name my-stack \
            --template-file instance.yaml \


workflows:
  starting-up-instance:
    jobs:
      - instance_creation